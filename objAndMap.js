// 🧠 Object and Map Operations
// Merge two objects with unique keys.

// Check if an object is empty.

// Count the number of properties in an object.

// Clone an object deeply.

// Check if two objects are equal.

// Get all keys of an object.

// Get all values of an object.

// Invert the keys and values of an object.

// Remove a property from an object.

// Check if an object has a specific property.

// Convert an object to an array of key-value pairs.

// Convert an array of key-value pairs to an object.

// Find the length of the longest key in an object.

// Group array elements by a given property.

// Check if all object properties have truthy values.

// Flatten a nested object.

// Create an object from two arrays (keys and values).

// Sum the values of all numeric properties in an object.

// Find the property with the highest value in an object.

// Convert a Map to an object.